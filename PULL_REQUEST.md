# Pull Request: 系统鲁棒性增强、性能优化与测试补全

## 📝 变更概述

本 PR 是一次全方位的工程化治理，旨在解决 `Md2Design` 项目中积累的类型安全、渲染性能、环境兼容性及基建缺失问题。

## ✨ 主要改进

### 1. 性能引擎重构

- **卡片延迟加载 (Lazy Rendering)**：针对超长 Markdown 内容，引入 `IntersectionObserver` 实现了视口感知的卡片渲染。极大地减少了首屏内存占用和滚动卡顿。
- **分页算法 (Pagination) 2.0**：重构了分页计算逻辑，支持原生数组返回，并修复了手动分页符 `---` 的识别逻辑，提升了内容分割的精准度。

### 2. 类型安全与质量防御 (Standard Linting)

- **消除 `any` 类型**：针对 `Sidebar`、`TopBar`、`ChangelogModal` 等核心组件进行了深度的类型重构，删除了所有隐式和不安全的类型转换。
- **测试补全 (Vitest)**：搭建了完整的 Vitest + React Testing Library 测试环境，并补全了 `Store` 核心逻辑与 `Pagination` 算法的 8 项单元测试。

### 3. 生产与容器化 (Docker & Build)

- **Docker 重新部署**：更新并验证了多阶段构建流程。
- **路径 404 修复**：在 Docker/生产环境下自动切换 Vite 基础路径为 `./`，确保在任何 IP 或子路径下资源加载正常。
- **防御性兼容**：修复了 `Store` 在非浏览器（如测试环境、SSR 预加载）下因引用 `document` 导致的崩溃问题。

### 4. 深度清理 (Cleanup)

- 移除了冗余的 `package-lock.json`、`deploy.bat` 以及旧版开发脚本。
- 全量执行了代码格式化 (`eslint --fix`)。

## ✅ 验证报告

- **Lint**: 0 错误, 0 警告
- **Build**: `bun run build` 成功通过
- **Test**: 8/8 Tests Passed (100% 覆盖关键路径)
- **Docker**: 容器运行稳健，Nginx 响应正常

## 📸 运行预览

> [!TIP]
> 建议在合并后运行 `docker-compose up -d --build` 更新生产环境镜像。
